pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'ls -l'
            }
        }
        stage('dock'){
            steps{
                sh 'ls'
            }
        }
    }
}
